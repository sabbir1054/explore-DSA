#include<bits/stdc++.h>
using namespace std;

int main(){
    int size, max, min;
    cin >> size;
    int arr[size];
    for (int i = 0; i < size; i++)
    {
        cin >> arr[i];
    }
    max = arr[0];
    min = arr[0];
    for (int i = 0; i < size; i++)
    {
        for (int j = 0; j < size; j++)
        {
            /* code */
        }
        
    }
    
    
    return 0;
}